<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#0099cc"
    tools:context="com.example.team6.uniclash.GameView">

    <!-- The primary full-screen view. This can be replaced with whatever view
         is needed to present your content, e.g. VideoView, SurfaceView,
         TextureView, etc. -->

    <!-- This FrameLayout insets its children based on system windows using
         android:fitsSystemWindows. -->

    <GridLayout android:layout_height="match_parent"
        android:layout_width="match_parent"
        android:rowCount="3"
        android:columnCount="3">

        <Button
            android:id="@+id/waveButton"
            android:layout_height="46dp"
            android:layout_column="1"
            android:layout_marginLeft="75dp"
            android:layout_row="0"
            android:onClick="pressWaveButton"
            android:text="Wave #" />

        <Button
            android:id="@+id/pauseButton"
            android:layout_height="46dp"
            android:layout_column="2"
            android:layout_marginLeft="100dp"
            android:layout_row="0"
            android:onClick="pressPauseButton"
            android:text="Pause" />

        <Button
            android:id="@+id/shopButton"
            android:layout_height="46dp"
            android:layout_column="0"
            android:layout_row="2"
            android:onClick="pressShopButton"
            android:text="Shop" />

        <Button
            android:id="@+id/startWaveButton"
            android:layout_height="46dp"
            android:layout_column="1"
            android:layout_marginLeft="150dp"
            android:layout_row="2"
            android:onClick="pressWaveButton"
            android:text="Start Wave" />

    </GridLayout>

</FrameLayout>
